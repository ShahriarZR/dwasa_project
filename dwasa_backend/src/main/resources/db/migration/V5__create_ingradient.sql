CREATE TABLE INGRADIENT (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(150) NOT NULL,
    REMARKS CLOB,
    FULL_NAME VARCHAR2(255) NOT NULL,
    AREA_OF_USE NUMBER(10) NOT NULL
);