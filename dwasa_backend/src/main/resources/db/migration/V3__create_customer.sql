CREATE TABLE CUSTOMER (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(150) NOT NULL,
    EMAIL VARCHAR2(255),
    PHONE VARCHAR2(50),
    TYPE VARCHAR2(30) NOT NULL,
    CONTACT_PERSON VARCHAR2(150),
    REMARKS CLOB,
    STATUS VARCHAR2(20) DEFAULT 'ACTIVE' NOT NULL,
    ADDRESS CLOB,
    SHOP_LOCATION VARCHAR2(255),
    CREATED_AT TIMESTAMP WITH TIME ZONE DEFAULT SYSTIMESTAMP NOT NULL,
    CREATED_BY NUMBER(19),
    UPDATED_AT TIMESTAMP WITH TIME ZONE DEFAULT SYSTIMESTAMP NOT NULL,
    UPDATED_BY NUMBER(19),
    DELETED_AT TIMESTAMP WITH TIME ZONE
);

ALTER TABLE CUSTOMER
    ADD CONSTRAINT CK_CUSTOMER_TYPE CHECK (TYPE IN ('RETAIL', 'WHOLESALE', 'CORPORATE', 'GOVERNMENT'));

ALTER TABLE CUSTOMER
    ADD CONSTRAINT CK_CUSTOMER_STATUS CHECK (STATUS IN ('ACTIVE', 'INACTIVE'));
